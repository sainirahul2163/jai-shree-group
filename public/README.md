# Public assets

Place the official Shree Perforators logo here as **`logo.png`** (PNG with transparent background, minimum 400px wide).

The site will automatically use this image in the header. If the file is missing, a text logo is shown instead.

The product catalogue PDF lives at `/brochure/jai-shree-brochure.pdf`.
